name: Validate
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate sources, generated BAT, compilation and argument handling
        shell: powershell
        run: ./tests/Test.ps1
